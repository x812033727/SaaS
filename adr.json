{
  "entries": [
    {
      "decision": "**Key 格式** — `myapp_` + `secrets.token_urlsafe(32)`，總長約 49 字元。",
      "rationale": "固定 prefix 方便 regex 掃描洩漏；`token_urlsafe(32)` 提供 256-bit 熵，無需額外防護。",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0560882
    },
    {
      "decision": "**key_prefix 取法** — 跳過固定 prefix，取隨機部分前 8 字元，即 `key[6:14]`，存入 `key_prefix` 欄位（加 index）。",
      "rationale": "原「前 8 字元」含 6 字元固定 `myapp_`，隨機部分只剩 2 字元（4096 種），縮候選集效果近乎無效；取隨機部分前 8 字元（`base64url`，≈ 64^8 ≈ 2^48 種），大量 key 時仍能有效縮候選集。",
      "rejected": "取整串前 8 字元（工程師與高級工程師均指出為地雷，強烈否決）。",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0560944
    },
    {
      "decision": "**Hash 方式** — `hashlib.sha256(key.encode()).hexdigest()`，無 salt，`key_hash` 加 UNIQUE index。",
      "rationale": "256-bit 隨機 key 本身高熵，無需 salt 防彩虹表；不加 salt 才能由輸入 key 直接反推 hash 查 DB。",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0560958
    },
    {
      "decision": "**ApiKey model 欄位** — `id / user_id(FK users) / tenant_id(FK tenants, index) / name / key_prefix(8 char, index) / key_hash(64 char, unique) / is_active(bool) / created_at`。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0560968
    },
    {
      "decision": "**ApiKeyUsage 獨立表** — `(id, api_key_id FK api_keys, tenant_id FK tenants, period Date, count)`，`UniqueConstraint(\"api_key_id\", \"period\")`，`api_key_id` 非 nullable。",
      "rationale": "SQLite 與 PostgreSQL 的 UNIQUE constraint 中 NULL ≠ NULL，若在 `ApiUsage` 加 nullable `api_key_id` 改 constraint，舊 tenant-level 列無法被唯一鍵攔住重複，`_get_or_create_usage_locked` 邏輯直接炸裂。",
      "rejected": "修改 `ApiUsage` 加 nullable `api_key_id`（NULL uniqueness 無解，不可行）。",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.056098
    },
    {
      "decision": "**`ApiUsage` 維持原樣** — 欄位、constraint、`check_and_increment` 邏輯全部零改動，現有 tenant-level quota 行為完整保留。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561004
    },
    {
      "decision": "**period 型別** — `ApiKeyUsage.period` 與 `ApiUsage.period` 均用 SQLAlchemy `Date` 型別（`datetime.date`），Python 層統一傳 `datetime.date` 物件，避免 `/usage` 聚合兩表時跨格式轉換。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.056101
    },
    {
      "decision": "**Actor dataclass** — `@dataclass class Actor: user: User; api_key_id: Optional[int] = None`，置於 `auth/dependencies.py`。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.056102
    },
    {
      "decision": "**dependency 串接** — `get_current_actor` 是新核心 dependency（回傳 `Actor`）；`get_current_user` 包裝它只回傳 `actor.user`（router 零改動）；`require_quota` 改 depends `get_current_actor`，取得 `api_key_id` 後仍回傳 `User`。FastAPI 同一 request 內同一 dependency 只呼叫一次，`get_current_actor` 定義為一般 `def`（非 `async def`）確保快取行為一致。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561032
    },
    {
      "decision": "**認證三路 if/elif/else** — `X-API-Key` header 有值 → API key 路徑；`Authorization: Bearer myapp_*` → API key 路徑；`Authorization: Bearer eyJ*` → JWT 路徑；三者皆無或皆無效 → 401。條件必須互斥且窮舉，測試須覆蓋「三者皆無」確認不回 200。",
      "rationale": "",
      "rejected": "巢狀 try/except 隱式判斷（條件不互斥，易漏接）。",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.056104
    },
    {
      "decision": "**`check_and_increment_key` 失敗行為** — `ApiKeyUsage` 寫入拋例外一律向上傳播（回 500），不靜默吞掉。",
      "rationale": "靜默失敗會造成計量偶發性漏記，形成不可見的隱性 bug；MVP 寧可明確 500 也不要無聲計量錯誤。",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561047
    },
    {
      "decision": "**quota 責任切分** — tenant-level 上限判定在 `check_and_increment`（ApiUsage），超量回 429；per-key 只累計計數（ApiKeyUsage），不設額外上限；MVP 階段無 per-key 配額需求。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561054
    },
    {
      "decision": "**撤銷** — `is_active=False` 軟刪除；auth lookup 加 `ApiKey.is_active == True` 過濾，撤銷後下一次請求即失效；`ApiKeyUsage` 外鍵保留歷史。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561063
    },
    {
      "decision": "**跨租戶隔離** — key lookup 驗 `ApiKey.tenant_id == actor.user.tenant_id`；`/api-keys` 與 `/usage` 均只查自身 tenant 資料；跨租戶回 403。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561068
    },
    {
      "decision": "**建立端點回應** — `POST /api-keys` 回傳 `plain_key`（完整明文，唯一一次）+ 其餘欄位；`GET /api-keys` 只回 `key_prefix`，不含 hash 或明文。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561075
    },
    {
      "decision": "**/usage 回應結構** — `{ tenant: { plan, daily_limit, used_today, remaining, period }, api_keys: [{ api_key_id, name, key_prefix, used_today, period }] }`；欄位名寫入 README。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561082
    },
    {
      "decision": "**新 router 掛載** — `api_keys.router` → `/api-keys`；`usage.router` → `/usage`；`app.py` lifespan 的 `init_db` 補 import 兩個新 model。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561087
    },
    {
      "decision": "**`init_db` import 順序** — `db.py` 的 `init_db` 明確列出所有 model import（`api_key`, `api_key_usage`），`create_all()` 前必須 import，順序：被依賴的 model（`ApiKey`）先於依賴它的（`ApiKeyUsage`）。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561097
    },
    {
      "decision": "**不引入新外部依賴** — 僅用 `hashlib`、`secrets`（stdlib）與 FastAPI 內建 `fastapi.security.APIKeyHeader`；`OAuth2PasswordBearer` 改 `auto_error=False`。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561106
    },
    {
      "decision": "**測試策略** — `conftest.py` 新增 `test_api_key` fixture（含明文 key）；覆蓋：key 建立/撤銷、`X-API-Key` + Bearer 雙標頭、用量累加可手算核對、429 超量、跨租戶 403、「三者皆無→401」；既有測試集不動；全部離線 in-memory SQLite。",
      "rationale": "",
      "rejected": "",
      "session_id": "de531e66d29e",
      "created_at": 1781367790.0561118
    },
    {
      "decision": "`PlanChangeHistory` model 欄位：`(id PK, tenant_id FK non-null, from_plan, to_plan, changed_by_user_id FK nullable, changed_at UTC non-null, reason nullable)`。",
      "rationale": "`changed_by_user_id` 允許 NULL 以應對 API key 認證（見下）。",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572623
    },
    {
      "decision": "actor 為 API key 認證時，`PlanChangeHistory.changed_by_user_id` 填入 `actor.user.id`（API key 所屬 User），不填 NULL。",
      "rationale": "API key 對應的 `Actor.user` 已在 `get_current_actor` 解析完畢，可直接取 user.id；避免歷程出現無法追溯的空值。",
      "rejected": "填 NULL ── 日後 admin 查歷程無法追溯是誰透過 API key 觸發了降級。",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726275
    },
    {
      "decision": "`User.is_admin = Column(Boolean, default=False, nullable=False)`；`Tenant.is_active = Column(Boolean, default=True, nullable=False)`。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572629
    },
    {
      "decision": "停用租戶（`is_active=False`）不建立 audit trail，屬已知技術債，在程式碼加 `# TODO: add TenantAuditLog if compliance needed`。",
      "rationale": "現階段無 compliance 需求，不過度設計；高級工程師意見已知悉，決策有意識地暫緩。",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726297
    },
    {
      "decision": "`init_db` import 順序：`Tenant` → `User` → `ApiKey` → `ApiKeyUsage` → `ApiUsage` → `PlanChangeHistory`。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726306
    },
    {
      "decision": "降級超量檢查移入 transaction 內，使用 `SELECT ... FOR UPDATE`（`query.with_for_update()`）鎖住 `ApiUsage` row 後再讀 count；若 `count > PLAN_DAILY_LIMITS[new_plan]` 則 rollback 並回 409，否則同一 transaction 寫 `Tenant.plan` 與 `PlanChangeHistory`。",
      "rationale": "消除 TOCTOU race window；單 worker 鎖無效能負擔，多 worker 亦可正確序列化。",
      "rejected": "transaction 外先查再寫 ── 並發降級請求可同時通過檢查，工程師與高級工程師均已指出此缺陷。",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572632
    },
    {
      "decision": "`services/billing.py` 的 `downgrade_plan(db, tenant, new_plan, actor_user_id)` 在同一 db session 內完成：`with_for_update()` 讀 usage → 超量 raise 409 ValueError → 否則更新 plan + insert history → commit。router 捕捉 ValueError 轉成 HTTP 409。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726328
    },
    {
      "decision": "模擬 payment_id 格式：`\"simulated_\" + secrets.token_hex(6)`（12 hex chars），完全 stdlib，無外部請求。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726335
    },
    {
      "decision": "`get_current_actor` 查 User 時改用 `joinedload(User.tenant)`，一次 SQL 取齊 user + tenant，避免 lazy chain `actor.user.tenant.is_active` 觸發 N+1 或 `DetachedInstanceError`。",
      "rationale": "高級工程師與工程師均指出此風險；eager load 無額外 round-trip，成本低。",
      "rejected": "依賴 SQLAlchemy lazy load ── session 若已關閉直接拋例外，且每請求多一次 DB query。",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726345
    },
    {
      "decision": "租戶停用攔截點：`get_current_actor` 組裝 `Actor` 前，若 `user.tenant.is_active == False` 則 `raise HTTPException(status_code=403, detail=\"tenant disabled\")`；admin 使用者不豁免（停用即全停）。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726354
    },
    {
      "decision": "`SlidingWindowRateLimiter.__init__` 新增 `clock: Callable[[], float] = time.monotonic`，存 `self._clock`，內部所有 `time.monotonic()` 改為 `self._clock()`；既有 instance 不傳 clock，預設行為不變。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726361
    },
    {
      "decision": "Per-key dict 不加 LRU，但在 `_log` dict 初始化處加註 `# TODO: replace with LRU(maxsize=10_000) before multi-tenant scale` 明確記錄技術債。",
      "rationale": "當前規模無問題；加 LRU 引入額外依賴不值得現在做。",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572637
    },
    {
      "decision": "Rate limiter 在 README 與部署說明區段明確標注「**單 worker 限定**：in-memory dict 不跨 process 共享，多 worker 需改 Redis 後端」。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572638
    },
    {
      "decision": "`require_rate_limit` 依賴判斷優先順序：`actor.api_key_id` 不為 None → per-key limiter；否則 → per-tenant limiter（key 為 `tenant_id`）；兩個 limiter 為 module-level singleton，env var `SAAS_KEY_RATE_LIMIT` / `SAAS_TENANT_RATE_LIMIT` 解析格式 `\"{calls}/{window_seconds}\"`。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726416
    },
    {
      "decision": "429 response 帶 `Retry-After: {window_seconds}` header（純整數），透過 `HTTPException(headers={\"Retry-After\": str(window)})` 回傳；不帶 `X-RateLimit-*` 系列 header（非本次需求範圍）。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726423
    },
    {
      "decision": "`require_admin` 為獨立 dependency（`deps.py` 匯出），`Depends(get_current_actor)` 後檢查 `actor.user.is_admin`；非 admin 回 `403 Forbidden`，不回 401。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572643
    },
    {
      "decision": "`routers/admin.py` 整個 `APIRouter` 掛 `dependencies=[Depends(require_admin)]`，所有子路由自動繼承，不需逐一宣告。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726438
    },
    {
      "decision": "`PATCH /admin/tenants/{id}` body `{is_active?: bool, plan?: str}`；改方案時複用 `billing.upgrade_plan` / `billing.downgrade_plan`（不重複邏輯）；停用/啟用僅寫 `Tenant.is_active`，不呼叫 billing service。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572645
    },
    {
      "decision": "新增三個測試檔：`tests/test_billing.py`、`tests/test_ratelimit_enhanced.py`、`tests/test_admin.py`；既有測試檔不改。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572646
    },
    {
      "decision": "`FakeClock` 實作為 `conftest.py` 中的 class，`__call__` 回傳 `self.t`，`.advance(n)` 遞增；測試傳入 `SlidingWindowRateLimiter(max_calls=N, window_seconds=W, clock=fake_clock)`，無任何 `time.sleep`。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.572656
    },
    {
      "decision": "`conftest.py` 新增 `admin_user` fixture（`is_admin=True`）與 `admin_token` fixture；`test_api_key`、`test_tenant` 等既有 fixture 不動。",
      "rationale": "",
      "rejected": "",
      "session_id": "7efdfe141c58",
      "created_at": 1781376697.5726566
    },
    {
      "decision": "`DeepLTranslator.translate()` 開頭一行 `norm = self._normalize_target_lang(target_lang)`，後續 API payload 與 skip 比較**均使用 `norm`**，消除兩處各自 `.upper()` 造成的不一致風險",
      "rationale": "工程師指出若漏改一處，繁中 400 或 skip 比較結果會靜默錯誤；單一變數是最低成本的防呆",
      "rejected": "",
      "session_id": "pjc9b4ab0b44",
      "created_at": 1781418287.5562677
    },
    {
      "decision": "`_normalize_target_lang()` 為 `DeepLTranslator` 的 `@staticmethod`，白名單映射 `{\"ZH-TW\": \"ZH-HANT\", \"ZH-CN\": \"ZH-HANS\"}`，其餘 `.upper()` 不變；不污染 `Translator` ABC 介面",
      "rationale": "",
      "rejected": "",
      "session_id": "pjc9b4ab0b44",
      "created_at": 1781418287.5562747
    },
    {
      "decision": "同語言 skip 判斷點維持「API 回應後讀 `detected_source_language`」；設計依據調整為「避免把同語言翻譯結果回覆給用戶（UX 正確性）」，**移除「省 DeepL 字符 quota」的主張**",
      "rationale": "高工疑慮 1 成立——HTTP 已發出、DeepL 是否計費未確認；功能本身仍合理，只是依據需更正，避免日後誤導",
      "rejected": "「skip 前不送 API 以省 quota」——需要額外語言偵測 API call，複雜度增加且不在本輪範圍",
      "session_id": "pjc9b4ab0b44",
      "created_at": 1781418287.5562766
    },
    {
      "decision": "在 skip 比較邏輯旁加程式碼註記：「DeepL 對中文偵測回傳 `ZH`，正規化後的 target 為 `ZH-HANT`，兩者不等，繁中→繁中不觸發 skip；此為 DeepL API 行為限制，非 bug」",
      "rationale": "工程師指出此限制若不記錄，日後排查會誤判 skip 無效",
      "rejected": "",
      "session_id": "pjc9b4ab0b44",
      "created_at": 1781418287.5562782
    },
    {
      "decision": "`StubTranslator(source_lang: str | None = None)` 建構子；skip 比較用單純 `target_lang.upper() == source_lang.upper()`，**不引入 `_normalize_target_lang`**；測試碼須使用能 `.upper()` 相等的語言碼（如 `\"JA\"/\"JA\"` 或 `\"ZH-HANT\"/\"ZH-HANT\"`），不用 `ZH-TW` 對 `ZH-HANT`",
      "rationale": "Stub 只驗 webhook 下游流程，不需複製 DeepL 特定正規化邏輯；在 PR 描述備注此侷限即可",
      "rejected": "Stub 內引入正規化映射表——過度複製 DeepL 實作細節至測試替身",
      "session_id": "pjc9b4ab0b44",
      "created_at": 1781418287.5562801
    },
    {
      "decision": "Webhook handler（`async def line_webhook`）內翻譯呼叫改為 `translated = await asyncio.to_thread(translator.translate, text, lang)`；`line_client.reply()` 同為阻塞但本輪不動，加 `# NOTE: blocking — wrap in asyncio.to_thread for high-traffic (M2)` 技術債標記",
      "rationale": "工程師確認 handler 為 `async def`，一行修改低風險；TestClient 下 thread pool 可正常運作",
      "rejected": "",
      "session_id": "pjc9b4ab0b44",
      "created_at": 1781418287.5562816
    },
    {
      "decision": "新增 `tests/test_translation_enhanced.py`，覆蓋：`_normalize_target_lang` 直接靜態呼叫（`ZH-TW→ZH-HANT`、`ZH-CN→ZH-HANS`、`JA→JA`）、`DeepLTranslator.translate()` 以 `unittest.mock.patch` 替換 urllib 模擬回應（正常翻譯與 skip 兩條路徑）、`StubTranslator` skip 行為；不改任何既有測試檔",
      "rationale": "",
      "rejected": "",
      "session_id": "pjc9b4ab0b44",
      "created_at": 1781418287.5562832
    },
    {
      "decision": "所有新端點加入 `routers/tenants.py`，`app.py` 不動。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.5988295
    },
    {
      "decision": "`services/line_config.py` 三函數原封不動，router 只做組裝呼叫。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.598835
    },
    {
      "decision": "三端點路徑均為 `/tenants/me/line-config`，無 `{tenant_id}` path param；`tenant_id` 唯一來源是 `current_user.tenant_id`。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.598838
    },
    {
      "decision": "使用現有 `get_current_user` dependency，不需 `require_admin`；admin 端點原狀不動。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.5988393
    },
    {
      "decision": "在 `routers/tenants.py` 定義 Pydantic response model `TenantLineConfigResponse`，欄位：`tenant_id / has_channel_secret / has_access_token / default_target_lang / created_at / updated_at / webhook_url`；`created_at / updated_at` 宣告為 `str | None`（service 層已 `.isoformat()` 序列化，非 datetime 物件）。",
      "rationale": "避免 Pydantic 對 str 重複序列化造成語意混淆。",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.5988438
    },
    {
      "decision": "`GET` 與 `PUT`（upsert 成功）均回傳完整 `TenantLineConfigResponse`（HTTP 200）；`DELETE` 回 **HTTP 204 No Content**。",
      "rationale": "204 對齊 self-service 同層級慣例（`notes.py`、`api_keys.py` 皆 204），非對齊 admin 端點（admin DELETE 實際回 200 + body，兩套契約本來就允許不同）。",
      "rejected": "「DELETE 回 200 + body 以對齊 admin」——admin 是後台契約；self-service 面向租戶，應與同層 REST 慣例一致。",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.5988457
    },
    {
      "decision": "`webhook_url` 值為相對路徑 `f\"/line/webhook/{tenant_id}\"`，router 以 `{**svc_dict, \"webhook_url\": f\"/line/webhook/{tenant_id}\"}` 組裝後傳入 response model。",
      "rationale": "host 因部署環境不同無法硬編碼，README 說明拼接方式。",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.5988472
    },
    {
      "decision": "錯誤處理全部由既有 HTTPException 直傳——`InvalidTargetLangError` 已在 service 層 L70-72 內部轉成 HTTPException(400)，router **不另加 try/except**，寫了是死代碼。",
      "rationale": "",
      "rejected": "「router 捕獲 InvalidTargetLangError 回 400」——service 層已處理，此寫法不可達。",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.598872
    },
    {
      "decision": "越權測試語意落地為：租戶 A 的 token 打 `GET /tenants/me/line-config`，若 A 無設定則回 A 的 **404**（而非洩漏 B 的資料）；無法透過此端點表達「A 直接操作 B」，隔離保障由「路徑無 param、token 強綁 tenant_id」結構性保證。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.5988734
    },
    {
      "decision": "新增 `tests/test_self_service_line_config.py`，覆蓋：CRUD happy path、`webhook_url` 欄位值、A token → GET/PUT/DELETE 回 A 的 404（非 B 的資料）、未設定時 GET 回 404。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.598875
    },
    {
      "decision": "本輪不改 admin 端點回應格式、不新增 secret/token 明文欄位、不實作 absolute URL 組裝（M2 backlog）。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj45e6462dcc",
      "created_at": 1781421359.5988762
    },
    {
      "decision": "**`line_bot_user_id` 欄位**：`models/line_channel_config.py` 加 `line_bot_user_id = Column(String(64), nullable=True, unique=True, index=True)`；`unique=True` 由 SQLAlchemy 產生獨立 index，非 inline constraint。",
      "rationale": "nullable 保向後相容；分離 index 利於 ALTER 拆步執行。",
      "rejected": "",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.5511432
    },
    {
      "decision": "**Schema 遷移（無 Alembic）**：`db.py` 的 `init_db()` 尾端加 `_migrate_add_line_bot_user_id()`；邏輯為「`sqlalchemy.inspect` 先查欄位存在性 → 缺則 `ALTER TABLE line_channel_configs ADD COLUMN line_bot_user_id VARCHAR(64)` → 再 `CREATE UNIQUE INDEX IF NOT EXISTS ix_lcfg_lbuid ON line_channel_configs(line_bot_user_id)`」；兩條 SQL 分開執行；整段 `try/except` 包住，失敗僅 `logger.warning`，不阻啟動。",
      "rationale": "拆成 ADD COLUMN + 獨立 CREATE INDEX，規避 SQLite 老版本 ALTER 不支援 inline UNIQUE 的問題；新欄位初始全為 NULL，SQLite unique index 允許多 NULL，migration 前無重複實值疑慮，工程師已確認安全。",
      "rejected": "直接 ALTER TABLE ADD COLUMN line_bot_user_id VARCHAR(64) UNIQUE —— SQLite 老版本 inline UNIQUE 支援不穩定，拆步更可移植。",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.551147
    },
    {
      "decision": "**`LineBotInfoClient` 介面用 `ABC` + `@abstractmethod`**，不用 `Protocol`；與既有 `LineReplyClient`（`ABC`）統一。",
      "rationale": "Protocol subclass 不實作方法在 runtime 才炸；ABC 在實例化時即報錯，保護更早。",
      "rejected": "Protocol —— 與既有慣例不一致，且少了 abstractmethod 的靜態保護。",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.551148
    },
    {
      "decision": "**`LineBotInfoClient` 實作放在既有 `line_client.py`**：新增 `HttpLineBotInfoClient`（stdlib `urllib`，`get_user_id(access_token) -> str | None`）與 `StubLineBotInfoClient(user_id: str | None)`（測試用）。",
      "rationale": "不引入新模組，與 `HttpLineReplyClient` / `FakeLineReplyClient` 同檔，維持現有「client 模組集中」慣例。",
      "rejected": "",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.551149
    },
    {
      "decision": "**`get_bot_info_client` FastAPI dependency**：定義於 `line_client.py`，回傳 `HttpLineBotInfoClient()`；測試透過 `app.dependency_overrides[get_bot_info_client] = lambda: StubLineBotInfoClient(...)` 注入，不用 monkeypatch。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.5511498
    },
    {
      "decision": "**upsert router 確認為 `def`（sync）**，服務內直接呼叫 `client.get_user_id()` 無需 `asyncio.to_thread()`；設計文件移除此 TODO。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.5511503
    },
    {
      "decision": "**`upsert_line_config()` 新增尾參數 `bot_info_client: LineBotInfoClient | None = None`**；在第一次 `db.commit()` + `db.refresh()` 成功後，執行下列固定模式：",
      "rationale": "IntegrityError 後若不 rollback，SQLAlchemy session 進入不可用狀態，會污染 connection pool 中的後續請求。",
      "rejected": "分開 try/except（先包 get_user_id，再單獨包 commit）—— 多一層結構複雜度，語意等價，統一包更清晰。",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.5511513
    },
    {
      "decision": "**Webhook destination 二次驗證**：位置在「HMAC 驗簽通過後、quota check 前」；條件 `if cfg.line_bot_user_id and payload.get(\"destination\") != cfg.line_bot_user_id`；失敗回 `JSONResponse(400, {\"detail\": _INVALID_SIGNATURE_DETAIL})`（共用同一常數）；log 訊息**不含 destination 值與 tenant_id**，僅中性描述如 `\"destination mismatch, rejecting webhook\"`。",
      "rationale": "共用常數防租戶列舉；log 不帶值防 log 側資訊洩漏，為高級工程師補充審查項。",
      "rejected": "自訂 detail 字串 —— 會形成可區分的 oracle，讓攻擊者能列舉租戶存在性。",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.5511525
    },
    {
      "decision": "**測試案例 ①（upsert 後驗 `line_bot_user_id` 被寫入）**：不靠 API response（`_to_response()` 未暴露此欄位），改為 `PUT` 後直接用 `_Session()` 查 ORM 物件，斷言 `cfg.line_bot_user_id == expected_user_id`；寫法對齊 `TestQuotaExceeded` 的 ORM 直查模式。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.5511532
    },
    {
      "decision": "**測試覆蓋四邊界**（跨兩個測試檔擴充）：① upsert + bot/info 成功 → ORM 直查 `line_bot_user_id` 有值；② upsert + bot/info 拋例外 → upsert 仍回 200、`line_bot_user_id` 為 None、session 無髒狀態；③ webhook destination 不符（`line_bot_user_id` 已設）→ 400 且 detail 與簽章失敗字串完全相同；④ `line_bot_user_id` 為 None（舊 config）→ destination 任意值仍 200 OK。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.551154
    },
    {
      "decision": "**本輪不改 `_to_response()` 暴露 `line_bot_user_id`**；該欄位為內部識別鍵，不需出現在租戶 API response，M2 視需求評估。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb57dcf61e7",
      "created_at": 1781423436.5511546
    },
    {
      "decision": "模組結構 — LineBotInfoClient 系列放在 line_client/ 套件，與 HttpLineReplyClient / FakeLineReplyClient 同層，維持「client 集中」慣例。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.1467009
    },
    {
      "decision": "Webhook 驗證順序固定為 raw body → DB query → 解密 → HMAC 驗簽 → JSON decode → destination 二次驗證 → quota 檢查 → 翻譯 → 回覆 → 計量。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.1467059
    },
    {
      "decision": "Quota 計費點在「翻譯+回覆皆成功後」（6d increment_usage）；失敗路徑不計量，消除白扣。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.146707
    },
    {
      "decision": "increment_usage 在 SELECT FOR UPDATE 鎖內重驗 count < limit，消除 has_quota → increment 的 TOCTOU 超賣窗口。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.14671
    },
    {
      "decision": "destination 二次驗證失敗與 HMAC 失敗共用同一 _INVALID_SIGNATURE_DETAIL 常數，防租戶列舉。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.1467133
    },
    {
      "decision": "HttpLineBotInfoClient.get_user_id() 讓例外向上傳播；由 upsert_line_config 的 try/except 統一吞掉並 rollback+refresh，不在 get_user_id 層 catch。",
      "rationale": "責任分離更清晰——client 只負責呼叫，例外語意由呼叫端決定；原設計文件描述「catch→return None」有誤，以此行更正。",
      "rejected": "在 get_user_id 內 catch 所有例外回傳 None——會讓 client 靜默吞掉不同類型例外（網路逾時 vs HTTP 4xx vs JSON 格式錯），呼叫端失去判斷能力。",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.1467147
    },
    {
      "decision": "技術債注釋寫在 quota.py，緊貼 require_quota 函式定義處（非 ARCHITECTURE.md，因該檔不存在）。",
      "rationale": "讀 code 的人需要在現場看到語意差異；散落文件檔的注釋實際上不會被讀到。",
      "rejected": "寫入 ARCHITECTURE.md——該檔不存在且須另建，溝通成本高；寫入 DECISIONS.md——只給決策閱讀者看，不在程式碼現場。",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.1467185
    },
    {
      "decision": "注釋內容須同時點明兩件事：① require_quota 採前扣語意（與 webhook 後扣並存）；② webhook 後扣存在「單次溢出」語意——has_quota 通過後若 increment_usage 鎖內重驗發現已達上限，該次翻譯/回覆已送出但不計量，屬設計可接受的一次性溢出，M2 統一後扣時須重新評估此邊界。",
      "rationale": "工程師指出若不寫明，M2 接手時容易誤判為 bug 或超賣。",
      "rejected": "",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.146723
    },
    {
      "decision": "reply() 同步阻塞、to_thread 包裝、line_bot_user_id 是否出 API，均標記 M2，本輪不處理。",
      "rationale": "",
      "rejected": "",
      "session_id": "pjb7bb61c982",
      "created_at": 1781426180.146724
    },
    {
      "decision": "抽出一個內部 helper `_constant_time_verify(body, signature: str, secret: str) -> bool`，把 `hmac.new + base64.b64encode + hmac.compare_digest` 三步驟封裝為單一實作點",
      "rationale": "三條拒絕路徑（cfg None / 缺 header / 簽章錯）共用此 helper 後，CPU 開銷強制對等；單一實作點也讓「未來換 signature scheme / 加 nonce」只需改一處",
      "rejected": "① 在三條路徑各別複製 b64encode+compare_digest——可被未來改動悄悄破壞對等性；② 把驗簽抽到獨立 module——過度設計，只在 router 內用一次",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.63689
    },
    {
      "decision": "cfg None 路徑改為呼叫 helper，傳入 `secret=b\"\\x00\"*32`（固定 dummy）與 `signature=request.headers.get(\"X-Line-Signature\", \"\")`（真實 header 值或空字串），再 `raise HTTPException(400, _INVALID_SIGNATURE_DETAIL)`",
      "rationale": "helper 內部固定跑完整 HMAC 鏈（new→digest→b64encode→compare_digest），dummy secret 讓結果必為 False，與簽章錯分支成本完全對等；放棄「cfg 缺失就只做最小運算」是刻意選擇——分析證明 sub-microsecond 成本可忽略",
      "rejected": "保留現狀「只跑 digest」——研究報告明確指出此差異構成可被遠端統計的 timing oracle",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.6368966
    },
    {
      "decision": "缺 header 路徑不再 short-circuit，改為「固定傳空字串進 helper」——`signature=\"\"` 走完整 HMAC 鏈，helper 自然回傳 False，再統一拋 400",
      "rationale": "把「缺 header」與「錯 header」合併為同一條 helper 呼叫，三條路徑的指令序列完全一致；這是 OWASP timing 對齊的最低成本實作",
      "rejected": "維持 `if not signature: raise` 短路——會在 timing 側留下明顯可觀測的 gap（短少整段 HMAC 計算）",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.6368978
    },
    {
      "decision": "簽章錯路徑也改走同一 helper（行為等價，但實作統一）；cfg None / 缺 header / 簽章錯三條路徑的最後一行都是 `raise HTTPException(400, _INVALID_SIGNATURE_DETAIL)`",
      "rationale": "「helper → raise」這個序列統一後，未來若有人想新增第四條路徑（如 destination 之外的二次驗證），compiler/type hint 會自然引導他走 helper；可逆性最大化",
      "rejected": "",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.6368988
    },
    {
      "decision": "三條拒絕路徑（含 destination 不符，總四條）內部各加一條 `_log.warning(\"webhook rejected reason=%s tenant=%d\", reason, tenant_id)`，reason 列舉 `no_config / missing_header / bad_signature / bad_destination`",
      "rationale": "OWASP 推薦模式——對外一致、對內區分（log 不對外暴露）；既不洩漏 detail 給攻擊者，也給監控/告警一個可靠的失敗原因欄位",
      "rejected": "① log 內容含 destination 值或完整 user_id——log 側資訊洩漏仍是洩漏；② 不分層只用 INFO 級——warning 級才能在 SIEM 觸發告警",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.6368995
    },
    {
      "decision": "新增測試 `test_all_rejection_paths_go_through_constant_time_helper`——用 `unittest.mock.patch` 監看 helper 呼叫次數與引數，斷言三條拒絕路徑**都呼叫 helper 一次**且引數形態對稱",
      "rationale": "wall-clock 計時在 CI 抖動過大不實用；改用「是否走同一 helper」當契約——比 timing 對齊更強的保證（成本對等是 helper 單一實作點的自然結果）；沿用既有「先 mock spy 再斷言」風格",
      "rejected": "① wall-clock timing 基準測試——CI 抖動讓測試不穩；② 純粹比較 response time 的鬆散斷言——會隨環境飄移",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.6369004
    },
    {
      "decision": "既有的 `test_configured_vs_unconfigured_indistinguishable_all_probes` 與 `test_mismatch_detail_identical_to_signature_failure` 全綠即視為「status code + body 統一」驗收；本輪不在這兩個斷言上做修改",
      "rationale": "既有測試已逐字節等價鎖死 status code 與 detail；本輪重點是 timing 對齊與 log 區分，兩者不相交",
      "rejected": "重寫或擴大既有等價斷言——風險高且無新合約覆蓋",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.6369011
    },
    {
      "decision": "不改既有契約——路由 `/line/webhook/{tenant_id}`、成功回應 `200 {\"status\":\"ok\"}`、`_INVALID_SIGNATURE_DETAIL = \"Invalid X-Line-Signature\"`、LineConfigDecryptionError→200、destination 二次驗證時機、計費點時機——皆保持現狀",
      "rationale": "PM 與研究員均確認這些契約已達標；本輪「只補缺口」，不擴大變更面",
      "rejected": "",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.6369016
    },
    {
      "decision": "既有架構決策中關於 `app/services/quota.py` 與 `app/api/line_bot_info.py` 的路徑記載與實際不符（實際為 `src/saas_mvp/quota.py`）——本輪沿用實際路徑，不翻案亦不發 PR 改既有架構決策文字",
      "rationale": "程式碼真相是檔案實際位置；翻案既有決策文件超出本輪 scope，且既有注釋已正確落在 `src/saas_mvp/quota.py` 函式定義處，符合「緊貼 require_quota」的語意",
      "rejected": "",
      "session_id": "pj6e9823c505",
      "created_at": 1781564583.6369023
    },
    {
      "decision": "字數計量模組邊界沿用 `quota.py`——`has_char_quota` 新增、`_get_or_create_usage_locked` 不動、SQLAlchemy `default=0` 自動補新 row 的 `char_count=0`；介面對稱、可逆性最大化。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122263
    },
    {
      "decision": "字數累計點採**譯文字數**（`len(translated)`），源文多語/表情歧義的鍋不該架構背；與既有後扣骨架自然對齊。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122294
    },
    {
      "decision": "字數計算採 Python `len(str)`（Unicode code point），與中文「字」語意對齊、手算可核對。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122303
    },
    {
      "decision": "字數上限常數新增 `PLAN_DAILY_CHAR_LIMITS: dict[str, int]`，**未知 plan 一律 fallback 到 `PLAN_DAILY_CHAR_LIMITS[\"free\"]`**，與既有 `PLAN_DAILY_LIMITS` 的 fallback 語意對齊——這是策略明文化，非細節。",
      "rationale": "對齊既有契約比「未知 plan 拒絕 vs 放行」的爭論更值錢；PM 之後若要 enterprise plan，加一條 dict 條目即可。",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.312231
    },
    {
      "decision": "webhook 後扣路徑並列兩道 read 閘——`has_quota` 通過後才進 `has_char_quota`，任一不通都回 `_QUOTA_EXCEEDED_MSG` 並 `continue`；兩閘獨立查詢、獨立擋下，沿用「單次溢出可接受」語意。",
      "rationale": "兩次 read-only 查詢成本可忽略；合併原子讀取會破壞既有 `has_quota` 契約。",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122318
    },
    {
      "decision": "**翻案**——放棄「`increment_char_usage` 與 `increment_usage` 並列、各自 SELECT FOR UPDATE + 各自 commit」的初版設計，改為**單一 `increment_usage(db, tenant_id, plan=None, chars=0)`**，同 row 一次 `SELECT FOR UPDATE`、同 transaction 內 `count += 1; char_count += chars`、單一 commit。",
      "rationale": "既有 `increment_usage` 是內部函式無對外契約、破壞介面成本為零；合併後每次 webhook 少一輪 DB 往返 + 少一次 commit，鎖窗口由兩次壓成一次，TOCTOU 與 commit 失敗率同向下降。",
      "rejected": "初版「兩個獨立 increment 函式各自鎖」——介面對稱的視覺收益換不來一輪 round-trip + 一次 commit 的實質成本。",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122327
    },
    {
      "decision": "`ApiUsage` model 加 `char_count = Column(Integer, nullable=False, default=0)`；新 row 走 SQLAlchemy default，既有 NULL 列**雙重保險**——讀取端 `(row.char_count or 0)` 兜底 + 一次性 backfill 腳本 `UPDATE api_usage SET char_count = 0 WHERE char_count IS NULL`（SQLite/PG 通用 SQL，不擴大 schema 變更面，屬於資料修正）。",
      "rationale": "讀取端兜底保「API 對外不報錯」，backfill 腳本保「DB 內部對 BI/監控/聚合查詢也一致」；二者並行，後者才是根治，前者是保險。",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122337
    },
    {
      "decision": "`/quota/status` 與 `/usage/` 兩端點**新增** `used_chars` / `char_limit` / `remaining_chars` 三個平行的字數欄位；既有 `used` / `limit` / `remaining` / `used_today` / `daily_limit` 欄位名與次數語意不變，欄位說明寫入對應 router docstring，不靠測試碼當文件。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122342
    },
    {
      "decision": "per-key 層（`ApiKeyUsage`）不補字數欄位——議程未要求、既有「per-key 共享租戶配額」契約只需次數軸；擴大變更面不符「只補缺口」原則。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122346
    },
    {
      "decision": "既有 webhook 計費點時機不變——字數閘**插入在 `has_quota` 通過之後、翻譯之前**，`increment_usage(plan, chars=N)` **取代**舊的 `increment_usage(plan)`，單一呼叫完成次數+字數兩軸遞增；下游翻譯/回覆失敗拋出時 `increment` 不會被執行，雙軸皆無白扣。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122354
    },
    {
      "decision": "`increment_usage` 內加 `chars <= 0` 早退（直接 return row.char_count）——與既有 `validate_count` 守衛同形，避免空字串或異常輸入造成無意義鎖操作。",
      "rationale": "",
      "rejected": "不在路由層 / webhook 層守衛——守衛該集中在計費原語內，呼叫端無需各自重複。",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.312236
    },
    {
      "decision": "字數超額語意與則數完全對等——**不翻譯、不計量、回 200 + `_QUOTA_EXCEEDED_MSG`**；不拋 500、不拋 429，行為與 PM 議程結論一致。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122365
    },
    {
      "decision": "測試對齊既有 `tests/test_quota.py` 與 `tests/test_line_webhook.py` 風格；每個「擋下/不計」案例附**反向對照組**（未超額→正常翻譯且 `char_count` 恰增 N）；**必涵「兩道閘都超額」的 case**——calls 先超額時，第一道閘擋下後第二道 `has_char_quota` 不得誤觸 char 計量；既有則數測試零修改。",
      "rationale": "高級工程師點出「第一道擋下後第二道不應誤觸」是測試覆蓋盲點，必須補。",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.312237
    },
    {
      "decision": "「翻譯/回覆成功但 `increment_usage` 失敗 = 已服務未計費」是**既有**失敗模式（次數軸沿用至今），本輪字數軸不修，**PR 描述必須點名此已知模式 + 開 issue tracker 留待 M2**。",
      "rationale": "修這個需先定義「成功副作用邊界」（何時算服務完成、何時算未完成），跨整個 webhook 設計面，超出本輪 scope；明文化是當前最值錢的處置。",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.3122375
    },
    {
      "decision": "既有架構決策（簽章驗證鏈、四條拒絕路徑收斂、`_INVALID_SIGNATURE_DETAIL`、destination 二次驗證、LineConfigDecryptionError→200、後扣語意、計費點時機、router 路徑、成功回應格式）**全部沿用，本輪不翻案**。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj0cd569bbeb",
      "created_at": 1781568053.312238
    },
    {
      "decision": "技術選型 — 本輪拒做 asyncio.to_thread 包裝與 async HTTP client 改寫",
      "rationale": "BackgroundTasks 對 sync 函式自動 run_in_threadpool（Starlette 源碼事實），等同 asyncio.to_thread 效果；再加為冗餘雙重包裝（anyio 反模式）。async 化需整套介面破壞性重構（LineReplyClient async 化 + AsyncSession + httpx lifespan + 全 spy mock 重寫），M1 流量下 ROI 為負，屬 M2 範疇。",
      "rejected": "(a) _process_events 改 async def + asyncio.to_thread 包 line_client.reply：自造輪子、淨效果 = 原地踏步；(b) httpx.AsyncClient / AsyncMessagingApi 整套 async 化：介面破壞面過大、屬獨立 M2 重構。",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964798
    },
    {
      "decision": "模組切分 — 本輪只動 saas_mvp/routers/line_webhook.py（文件）與 tests/test_qa_task4_to_thread.py（測試收斂），零邏輯改動",
      "rationale": "",
      "rejected": "順手修 _process_events / HttpLineReplyClient / 計費點 / 簽章鏈——守住「只補缺口」原則，避免變更面擴大。",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964846
    },
    {
      "decision": "測試輔助擴展 — FakeLineReplyClient 新增可選 delay 屬性（reply 內 time.sleep），預設 0 維持既有行為",
      "rationale": "不變量測試需可注入阻塞；既有測試不需改、零契約破壞；M2 async 化可重用此機制。",
      "rejected": "",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964855
    },
    {
      "decision": "測試收斂 — TestToThreadWrapping 三個 test 改寫：移除 to_thread 攔截斷言、改以 thread ident 斷言「translate 跑在背景 threadpool」；test_result_correct_through_to_end 保留；test_non_text_event 改寫為「非文字事件不觸發翻譯」斷言",
      "rationale": "",
      "rejected": "保留舊名 test_translate_called_via_to_thread——名稱已誤導、與新語意不符。",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964865
    },
    {
      "decision": "測試檔 import 清理 — 移除測試檔的 `import asyncio` 與所有 `mock.patch.object(webhook_mod.asyncio, \"to_thread\", ...)` 呼叫",
      "rationale": "拒做 to_thread 後 line_webhook 模組不應 import asyncio，webhook_mod.asyncio 屬性查找會 AttributeError，整個 test 模組連 pytest collection 都過不了。",
      "rejected": "",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964872
    },
    {
      "decision": "架構不變量測試 — 新增 test_blocking_reply_does_not_block_handler：override SlowLineReplyClient（reply 內 time.sleep(0.5)），斷言 client.post(/line/webhook/...) 在 <0.3s 內回 200",
      "rationale": "把「BackgroundTasks 不阻塞 handler」升級為可被測試守住的不變量；守的是架構契約（handler 不等 reply）、不是「某 function 被呼叫」的實作細節。",
      "rejected": "0.2s——CI 容器 load 高時 threadpool dispatch 可能吃掉 50-100ms，margin 偏緊、flakiness 風險高。",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964881
    },
    {
      "decision": "文件化語意 — line_webhook.py 步驟 6c 註解改述「reply 阻塞 I/O 在 BackgroundTasks 機制下已自動被 run_in_threadpool 移出 event loop，等同 asyncio.to_thread 效果；threadpool 線程佔用（高 RPS × 多 worker）見 M2 async 化技術債」；模組 docstring M2 段落更新為「HttpLineReplyClient 改用 httpx.AsyncClient（lifespan 管理單一 instance）+ _process_events 改 async def + AsyncSession 整套重構；asyncio.to_thread 包裝為錯誤方向、不再列入技術債」",
      "rationale": "",
      "rejected": "",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964917
    },
    {
      "decision": "外部缺口處置 — 既有 7 個 test_line_task2_char_quota / test_qa_task3_webhook_char_metering 失敗加 @pytest.mark.xfail(reason=\"has_char_quota 簽名缺口，issue #XXX\") 或 skip，從 baseline 噪音中隔出；PR 描述明列 xfail count",
      "rationale": "直接合進 master 會讓 CI baseline 永久 7 紅，未來提 PR 的人分不清「已知缺口」vs「本次 regression」；xfail 讓 CI 綠、缺口可被追蹤、reviewer 一眼看清狀態。",
      "rejected": "(a) 留紅直接合——CI 噪音永久化；(b) 本輪順手修——超出本輪 scope、PR 失焦。",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964924
    },
    {
      "decision": "可逆性 — 本輪所有改動皆為文件 + 測試 + Fake 測試輔助屬性，無邏輯變更；M2 async 化若啟動，測試方法改名（to_thread→background threadpool 斷言）但 delay 機制與不變量測試可重用；M2 方向被否決時 revert 成本 = 改兩段 docstring + 幾個 test method + 移除 xfail marker。",
      "rationale": "",
      "rejected": "",
      "session_id": "pj414ec0cf17",
      "created_at": 1781589461.6964934
    }
  ]
}